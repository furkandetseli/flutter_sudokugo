name: sudoku_go
description: Birden fazla dil destekli bir Sudoku oyun uygulaması.

publish_to: 'none'

version: 1.0.5+5

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  google_mobile_ads: ^5.2.0
  intl: ^0.19.0
  shared_preferences: ^2.0.0
  url_launcher: ^6.1.10

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/sudoku_logo.png
    - assets/icon/icon.png
    - assets/ad.png
  uses-material-design: true
  generate: true

flutter_icons:
  image_path: "assets/icon/icon.png"
  android: true
  ios: true